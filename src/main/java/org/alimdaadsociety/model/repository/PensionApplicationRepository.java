package org.alimdaadsociety.model.repository;

import org.alimdaadsociety.model.bean.PensionApplication;
import org.springframework.data.repository.CrudRepository;

public interface PensionApplicationRepository extends CrudRepository<PensionApplication, Long> {

	
}
